function formatar(){ //Codigo com Erro, ainda aprendendo input
    let format = parseFloat(document.getElementById("valor"));
    let resultado = format * 0.85;
    window.alert("resultado:" + resultado);
}